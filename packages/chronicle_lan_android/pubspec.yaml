name: chronicle_lan_android
version: 0.1.0
publish_to: none
description: Android manifest bridge for Chronicle local pairing.

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: io.github.katcurl.chronicle_lan_android
        pluginClass: ChronicleLanAndroidPlugin
