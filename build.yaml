targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            chronicle: lib/data/database/chronicle_database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
