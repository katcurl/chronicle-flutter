name: chronicle
version: 0.7.2+9
publish_to: none
description: Local-first research, study, notes and time tracking workspace.

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_markdown: ^0.7.4+1
  flutter_math_fork: ^0.7.2
  intl: ^0.19.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
